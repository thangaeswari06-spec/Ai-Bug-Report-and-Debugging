# Day 1 - CodeBERT classifier
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
evaluate>=0.4.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Day 2 - Severity model
xgboost>=2.0.0
joblib>=1.3.0
matplotlib>=3.7.0

# Day 3 - LLM + RAG
requests>=2.31.0

# Day 4 - FastAPI backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Accounts, security, uploads (new)
PyJWT>=2.7.0            # login tokens
google-auth>=2.29.0     # verifies "Continue with Google" tokens
python-multipart>=0.0.9 # file uploads (avatar, error screenshot)
Pillow>=10.0.0          # image validation / re-encoding
pytesseract>=0.3.10     # OCR wrapper - also install the Tesseract engine (see backend/ai/ocr.py)
pytest>=8.0.0
httpx>=0.27.0           # used by the API tests
